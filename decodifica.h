/* Header da biblioteca "decodifica", cujo proposito
e decodificar uma mensagem por meio de uma lista de chaves. */

int decodifica_mensagem(lista *l, FILE *MensagemDecodificada, int *arr_codigo, int tamanho);